

export default function ProductListPage() {

    return(
        <div className="grid">
            <div className="stack">
                <h2 style={{margin: 0}}>product list</h2>
                <span className="pill">{latest.length} products in total</span>
            </div>

        </div>
    )
}
